{
  "name": "conference-app-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end tests for the conference voting app, run against the real cluster via mirrord.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
