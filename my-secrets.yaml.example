# Copy this file to my-secrets.yaml and fill in real values before deploying.
# my-secrets.yaml is gitignored — never commit the real file.
#
# Usage:
#   helm upgrade --install conference-app ./helm/conference-app \
#     --namespace conference-app \
#     --create-namespace \
#     -f my-secrets.yaml

secrets:
  postgresql:
    password: "changeme-postgres-password"
  slack:
    webhookUrl: "https://hooks.slack.com/services/YOUR/WEBHOOK/URL"
